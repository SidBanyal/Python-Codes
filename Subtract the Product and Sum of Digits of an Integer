class Solution:
    def subtractProductAndSum(self, n: int) -> int:
        x=1
        y=0
        while (n>0):
            digit=n%10
            x=x*digit
            y=y+digit
            n=n//10
        return x-y
