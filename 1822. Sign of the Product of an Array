class Solution(object):
    def arraySign(self, nums):
        k=1
        for i in range(0,len(nums)):
            k=k*nums[i]
        if (k<0):
            return -1
        if (k>0):
            return 1
        else:
            return 0
